<ci:ComponentBase xmlns="https://github.com/avaloniaui"
                  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                  xmlns:ci="http://classisland.tech/schemas/xaml/core"
                  xmlns:components="clr-namespace:ExtraIsland.Components"
                  xmlns:controls="clr-namespace:FluentAvalonia.UI.Controls;assembly=FluentAvalonia"
                  xmlns:converters="clr-namespace:ExtraIsland.Shared.Converters"
                  x:Class="ExtraIsland.Components.ProfileInformationSettings"
                  x:TypeArguments="components:ProfileInformationConfig">
    <ci:ComponentBase.Resources>
        <converters:EnumDescriptionConverter x:Key="EnumDescriptionConverter" />
    </ci:ComponentBase.Resources>
    <ScrollViewer
        DataContext="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=components:ProfileInformationSettings}}">
        <StackPanel Classes="settings-container animated-intro">
            <controls:SettingsExpander IconSource="{ci:FluentIconSource &#xE4B4;}"
                                       Header="信息类型"
                                       Description="选择要显示的信息类型">
                <controls:SettingsExpander.Footer>
                    <ComboBox
                        MinWidth="20"
                        SelectedItem="{Binding Settings.Type}"
                        ItemsSource="{Binding InformationTypes}">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Converter={StaticResource EnumDescriptionConverter}}" />
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                </controls:SettingsExpander.Footer>
            </controls:SettingsExpander>
            <controls:SettingsExpander IconSource="{ci:FluentIconSource &#xE486;}"
                                       Header="简洁模式"
                                       Description="启用时, 将只显示信息最关键的部分">
                <controls:SettingsExpander.Footer>
                    <ToggleSwitch IsChecked="{Binding Settings.IsShortModeEnabled, Mode=TwoWay}"/>
                </controls:SettingsExpander.Footer>
            </controls:SettingsExpander>
        </StackPanel>
    </ScrollViewer>
</ci:ComponentBase>